# Starling deal-board hunt lane — EMPTY ON PURPOSE.
#
# Sep 25 2026: the BSTJ grails that lived here moved into the hunt engine
# (scripts/hunt-engine/hunts.ts, GRAILS). They are searched first every tick,
# evaluated, persisted and shown on the Hunt view like every other hunt. An
# empty list is valid here (scripts/hunt.ts); the board's hunt strip hides
# itself when there are no targets.
